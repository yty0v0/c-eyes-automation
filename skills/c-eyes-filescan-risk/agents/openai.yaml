interface:
  display_name: "C-Eyes 文件风险扫描"
  short_description: "固定执行 filescan-risk workflow。"
  default_prompt: "必须严格保证不对扫描目标进行任何增删改查的操作。只允许执行只读安全扫描、证据采集、结果归档和本地结果分析。使用 $c-eyes-filescan-risk 时，要求提供 workspace 和 target-path，直接执行 filescan-risk workflow，并优先读取 summary.json。"
